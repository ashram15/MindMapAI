/* Home page entry that renders the interactive 3D research graph UI. */

import SearchGraph from '../components/Graph';

export default function Home() {
  return (
    <main>
      <SearchGraph />
    </main>
  );
}